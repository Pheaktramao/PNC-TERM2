def average(NumberOfStudent):
    Sumavr=0
    for i in range(len(NumberOfStudent)):
        sum=0
        for j in range(student):
            sum+=int(student[i])/(student)
        sum=sum/student
    Sumavr=sum/NumberOfStudent
    return Sumavr
student=eval(input())
NumberOfStudent=eval(input())
print(average(NumberOfStudent))